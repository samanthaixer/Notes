# Loop over the array until n > array.length
# need to check how we keep track of the end of the loop

# Take first, n = 0

# Compare to duplicates list

# Compare to next n + 1

# If array n == array n + 1, put the number in duplicates
# and remove cards from the array

# If array n != array n + 1, compare to n + 2

# If array n == array n + 2, put the number in duplicates
# and remove cards from the array

# (repeat)
